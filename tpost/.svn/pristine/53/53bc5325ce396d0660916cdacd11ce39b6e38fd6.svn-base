package tpost.SimpleTest.common;

/**
 * CHAR : 문자값을 위한 타입<br>
 * UNSIGNED_SHORT : UNSIGNED SHORT 를 위한 타입 (주의 : UNSIGNED 표현을 위해 내부에서는 INT 형으로 처리됨)<br>
 * SHORT : SHORT (4 bytes) 를 위한 타입 (참고 : 연동 규격서 특성상 거의 대부분 UNSIGNED_SHORT 사용함)<br>
 * UNSIGNED_INT : UNSIGNED INT 를 위한 타입 (주의 : UNSIGNED 표현을 위해 내부에서는 LONG 형으로 처리됨)<br>
 * INT : INT (8 bytes) 를 위한 타입 (참고 : 연동 규격서 특성상 거의 대부분 UNSIGNED_INT 사용함)<br>
 * FILE : 첨부파일 전송을 위한 필드 (파일의 절대 경로를 기입하면 데이터 전송 시 해당 위치를 읽어 파일내용을 바이너리로 전송함)
 */
public enum DataType {
	CHAR, UNSIGNED_SHORT, SHORT, UNSIGNED_INT, INT, FILE;
}
